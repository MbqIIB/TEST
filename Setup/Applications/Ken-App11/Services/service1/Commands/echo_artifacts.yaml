harnessApiVersion: '1.0'
type: OTHER
commandUnitType: COMMAND
commandUnits:
  - command: |-
        echo "this is artifact description"
        echo ${artifact.description}
        echo "this is artifact build.no"
        echo ${artifact.buildNo}
    commandUnitType: EXEC
    deploymentType: SSH
    name: Exec-0
targetToAllEnv: true
