harnessApiVersion: '1.0'
type: APPLICATION_DEFAULTS
defaults:
  - name: RUNTIME_PATH
    value: /tmp
  - name: WINGS_RUNTIME_PATH
    value: $HOME/${app.name}/${service.name}/${env.name}/runtime
  - name: KEN_PATH
    value: /tmp
  - name: BACKUP_PATH
    value: $HOME/${app.name}/${service.name}/${env.name}/backup/${timestampId}
